import { GetChatroomPipe } from './get-chatroom.pipe';

describe('GetChatroomPipe', () => {
  it('create an instance', () => {
    const pipe = new GetChatroomPipe();
    expect(pipe).toBeTruthy();
  });
});
