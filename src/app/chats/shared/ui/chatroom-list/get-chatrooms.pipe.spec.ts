import { GetChatroomsPipe } from './get-chatrooms.pipe';

describe('GetChatroomsPipe', () => {
  it('create an instance', () => {
    const pipe = new GetChatroomsPipe();
    expect(pipe).toBeTruthy();
  });
});
