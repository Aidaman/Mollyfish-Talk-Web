export enum ChatroomTypes {
	DIRECT_MESSAGES = 'direct-messages',
	GROUP_CHAT = 'group-chat',

	//! These are only planned for a future, for now only the ones above should be done
	LAKE_FEED = 'feed',
	CHANEL = 'chanel',
}
